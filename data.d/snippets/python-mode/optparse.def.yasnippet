# -*- mode: snippet -*-
# name: optparse.def
# 'r' charactor before the first triple-quote for doctest
# don't want escape sequences - e.g '\n'
# --
PROG_NAME = sys.argv[0]
def parsersetup():
    r"""Get options"""
    usage = 'Usage: ' + PROG_NAME + '${1:[options]}'
    parser = OptionParser(usage)
    parser.add_option(
        '-v', '--verbose',
        action='store_true',
        default=False,
        dest='verbose',
        help='a lot print')
    $0
    return parser
