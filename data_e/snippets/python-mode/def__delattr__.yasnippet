# -*- mode: snippet -*-
# name: __delattr__(self, name):
# key: defdelattr
# condition: (not (py:at-string|comment-p))
# expand-env: ((yas/indent-line 'fixed) (yas/wrap-around-region 'nil))

# __setattr__() に似ていますが、代入ではなく値の削除を行います。このメソッド
# を実装するのは、オブジェクトにとって "del obj.name" が意味がある場合だけに
# しなければなりません。
# --
def __delattr__(self, name)
    $0