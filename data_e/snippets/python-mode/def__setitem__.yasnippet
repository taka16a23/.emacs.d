# -*- mode: snippet -*-
# name: __setitem__(self, key, val):
# key: defsetitem
# condition: (not (py:at-string|comment-p))
# expand-env: ((yas/indent-line 'fixed) (yas/wrap-around-region 'nil))
#
# self[key] に対する代入を実現するために呼び出されます。 __getitem__() と同じ
# 注意事項があてはまります。このメソッドを実装できるのは、あるキーに対する値
# の変更をサポートしているか、新たなキーを追加できるようなマップの場合と、あ
# る要素を置き換えることができるシーケンスの場合だけです。不正な key に対して
# は、__getitem__() メソッドと同様の例外の送出を行わなければなりません。
# --
def __setitem__(self, ${1:key}, ${2:val}):
    $0