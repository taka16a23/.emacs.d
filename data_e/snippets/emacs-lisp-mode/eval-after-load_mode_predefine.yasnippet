# -*- mode: snippet -*-
# name: eval-after-load mode
# key: (eval-after-load
# condition: (not (at-string|comment-p))
# --
(defun $1-predefine ()
  "For `eval-after-load' $1 function."
  (message "Loading $1..")
  $0)

(eval-after-load '${1:filename} '($1-mode-predefine)